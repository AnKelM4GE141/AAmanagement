# Supabase Configuration
# Get these from your Supabase project settings: https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Stripe Configuration (Use TEST keys for development, LIVE keys for production)
# Get your API keys from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_... or sk_live_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_... or pk_live_...

# Stripe Webhooks
# For local development: Run `stripe listen --forward-to localhost:3000/api/webhooks/stripe`
# For production: Configure webhook endpoint in Stripe dashboard
STRIPE_WEBHOOK_SECRET=whsec_...

# Cron Job Security
# Generate a secure random string for production cron job authentication
# Use a password generator or run: openssl rand -base64 32
CRON_SECRET=your_secure_random_string
